---
import { formatDate } from '../../lib/blog';
interface Props { pubDate: Date; minutes: number; update?: number; class?: string; }
const { pubDate, minutes, update, class: className = '' } = Astro.props;
---
<div class:list={["flex flex-wrap items-center gap-x-2.5 gap-y-1.5 text-xs text-base-content/55", className]}>
  {update != null && (
    <span class="ubuntu-mono-regular uppercase tracking-wide text-accent">Update {String(update).padStart(2, '0')}</span>
  )}
  {update != null && <span aria-hidden="true" class="text-base-content/30">·</span>}
  <time datetime={pubDate.toISOString()} class="ubuntu-mono-regular uppercase tracking-wide">{formatDate(pubDate)}</time>
  <span aria-hidden="true" class="text-base-content/30">·</span>
  <span class="ubuntu-mono-regular">{minutes} min</span>
</div>
